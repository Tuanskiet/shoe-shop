type SiteMapType = {
    [key: string]: string;
};

const HOME_SITE_MAP: SiteMapType = {
    home: '/home',
    contact: '/contact',
    about: '/about',
    settings: '/settings',
};

export default HOME_SITE_MAP;
